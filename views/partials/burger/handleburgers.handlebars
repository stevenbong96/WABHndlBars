<li>
	<button class="change-status" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}{{burger_name}}{{else}}{{burger_name}}{{/if}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{this.id}}">DELETE!</button> --}}
</li>